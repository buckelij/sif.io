name: CI

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

steps:
  - uses: actions/checkout@v6
  - uses: actions/setup-go@v6
    with:
      go-version-file: 'go.mod'
  - run: go test ./...